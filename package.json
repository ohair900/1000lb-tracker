{
  "name": "1000lb-club-tracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node tests.js",
    "deploy": "vite build && cp dist/index.src.html index.html && rm -rf assets && cp -r dist/assets assets"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^4.1.2"
  }
}
